{
  "name": "magie-companion",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "lint": "eslint .",
    "smoke": "node scripts/smoke.js",
    "test": "npm run lint && npm run smoke"
  },
  "devDependencies": {
    "eslint": "^8.57.0"
  }
}
